#overview
  %h1 Listing images
  = link_to 'New Image', new_image_path

- @images.each do |image|
  .img
    = link_to image_tag(image.photo(:tiny)), edit_image_path(image)
    %br
    = link_to 'Edit', edit_image_path(image)
    |
    = link_to 'Destroy', image, :confirm => 'Are you sure?', :method => :delete

%br
