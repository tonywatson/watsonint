#overview.hero-unit
  %h1 Projects
  %p This is a compendium of my work. I designed most of the projects here, but programmed all of them.

%table
  %tr
    %th
    %th
    %th

  - @projects.each do |project|
    = render :partial => 'show', :locals => {:project => project}

%br

= link_to 'New Project', new_project_path
